#~/.bash_profile for hangbo's Mac

#PATH  setted in ~/.bashrc

#if [ -f $(brew --prefix)/etc/bash_completion ]; then
#  . $(brew --prefix)/etc/bash_completion
#fi

if [ -f ~/.git-completion.bash ]; then
  . ~/.git-completion.bash
fi

if [ -f ~/.bashrc ]; then
        . ~/.bashrc
fi
